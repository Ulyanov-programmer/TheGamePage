import{isNullOrWhiteSpaces}from"./general.js";export default class ScrollElement{constructor(e,l){if(isNullOrWhiteSpaces(e))throw new Error("[SCROLL-ELEMENTS] Incorrect scroll-buttons selector!");let t=document.querySelectorAll(e);for(let o of t)o.addEventListener("click",this.scrollToElement);if(0==isNullOrWhiteSpaces(l)){let e=document.querySelector(l).clientHeight;ScrollElement.fixedHeaderHeight=e}}scrollToElement(){let e=document.querySelector(this.dataset.scrollTo);if(null==e)throw new Error("[SCROLL-ELEMENTS] Something wrong with scrollElement!");let l=window.pageYOffset+e.getBoundingClientRect().top;window.scrollTo({top:l-ScrollElement.fixedHeaderHeight,behavior:"smooth"})}}ScrollElement.fixedHeaderHeight=0;